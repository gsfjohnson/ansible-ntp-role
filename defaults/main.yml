---

ntp_pkg_name: ntp
ntp_pkg_state: installed

ntp_svc_name: ntpd
ntp_svc_state: started
ntp_svc_enabled: true

ntp_servers:
  - host: 0.centos.pool.ntp.org
    params: iburst
  - host: 1.centos.pool.ntp.org
    params: iburst
  - host: 2.centos.pool.ntp.org
    params: iburst
  - host: 3.centos.pool.ntp.org
    params: iburst
